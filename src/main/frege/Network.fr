module me.fornever.jtray.Network where

import Java.Net (URL)

native streamToString org.apache.commons.io.IOUtils.toString :: URL -> IO (IOException|String)

getUrlContent :: String -> IO (IOException|String)
getUrlContent href = do
    url <- URL.new href
    content <- streamToString url
    return content
